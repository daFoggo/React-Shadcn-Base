import { Card } from "@/components/ui/card"

const AppointmentsRequests = () => {
    return (
        <Card className="w-full h-full bg-muted p-6">AppointmentsRequests</Card>
    )
}

export default AppointmentsRequests